import ChatHistory from "../ChatHistory/ChatHistory";
import ChatInput from "../ChatInput/ChatInput";

function Chat() {
  return (
    <>
      <ChatHistory/>
      <ChatInput/>
    </>
  );
}

export default Chat;
